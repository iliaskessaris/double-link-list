This program aims to define a node for use in Double Link Lists and 
then to provide the essential function to manage it. The node, in our
case, can store only an integer as data. We can easyly modify the node, 
and  consequently the whole program, to manage double link lists with
varius data types.